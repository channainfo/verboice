.toptitle
  %span.title= external_service.name
.middle
  = external_service.url
.bottom
  = link_to t('views.external_services._summary.action.edit'), '#', :class => 'farrow collapse_trigger'
  = link_to t('views.external_services._summary.action.update_manifest'), update_manifest_project_external_service_path(project.id, external_service.id), :method => :put, :class => 'fsync' if external_service.id