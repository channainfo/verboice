<h2><%= t('views.channels.new.label.new') %> <%= @channel.kind %> <%= t('views.channels.new.label.channel') %></h2>

<%= render 'form' %>

<%= link_to t('views.channels.new.action.back'), channels_path %>
